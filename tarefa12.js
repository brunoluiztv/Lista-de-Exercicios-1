// Inicia um loop 'for' que itera sobre os números de 1000 a 1999
for(let i = 1000; i <= 1999; i++) {
    // Verifica se o resto da divisão de 'i' por 11 é igual a 5
    if(i % 11 === 5) {
        // Se a condição for verdadeira, imprime 'i' no console
        console.log(i);
    }
}

